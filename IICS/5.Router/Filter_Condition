

  ISNULL(DIALING_CODE) OR
  ISNULL(IO_FIRST_NAME) OR
  ISNULL(IO_LAST_NAME) OR 
  ISNULL(IO_EMAIL) OR 
  ISNULL(IO_PHONE) OR 
  ISNULL(IDOB) OR  
  LENGTH(IO_PHONE) != 10 OR
IAGE_YEARS = -1 OR
NOT REG_MATCH(IO_EMAIL, '^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}$'
)
